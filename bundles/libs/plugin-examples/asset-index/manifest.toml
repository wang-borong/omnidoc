manifest_version = 1
key = "asset-index"
name = "Generated Asset Index"
version = "1.0.0"
compatible_omnidoc = ">=1.7.0,<2.0.0"
description = "Generates a stable Markdown inventory for files under assets/ and images/"
kind = "plugin"

[hooks]
asset_provider = ["{python}", "{plugin_dir}/scripts/generate.py"]
